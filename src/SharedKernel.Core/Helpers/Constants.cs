namespace Clean.Microservice.Serverless.SharedKernel.Core.Helpers
{
    /// <summary>
    /// Constants for Helpers
    /// </summary>
    public static class Constants
    {
        /// <summary>
        /// Regex only numbers
        /// </summary>
        public const string RegexOnlyNumbers = "[^0-9]";

        /// <summary>
        /// Regex only letters
        /// </summary>
        public const string RegexOnlyLetters = "[^a-zA-Z]";
    }
}
